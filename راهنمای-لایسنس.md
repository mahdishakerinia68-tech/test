# راهنمای سامانه لایسنس (نسخه ۲.۴)

## یک کار لازم در کنسول Firebase (یک‌بار)
برای این‌که پنل ادمین بتواند فهرست همه‌ی حساب‌ها را ببیند ولی کاربر عادی فقط
لایسنس خودش را، باید Firestore Rules پروژه `hesabdari-fd3a3` را تنظیم کنی:

1. به کنسول Firebase پروژه برو → Firestore Database → تب **Rules**.
2. کل محتوای فایل `firestore.rules` (همین پوشه) را کپی و جایگزین rules فعلی کن.
3. **Publish** بزن.

بدون این مرحله، خواندن فهرست حساب‌ها در پنل ادمین یا حتی خواندن لایسنس خود
کاربر با خطای دسترسی (`permission-denied`) مواجه می‌شود.

## ورود ادمین اصلی
ایمیل: `mahdishakerinia68@gmail.com`
رمز: `Mahdi66371335`

اولین بار که با همین ایمیل/رمز از تنظیمات → همگام‌سازی دو گوشی وارد شوی،
اگر این حساب هنوز در Firebase Auth ساخته نشده باشد، خودش ساخته می‌شود و
به‌صورت خودکار به‌عنوان ادمین دائمی ثبت می‌شود. بعد از آن، دکمه‌ی
«🛠 پنل مدیریت» زیر همان بخش ظاهر می‌شود.

## نکته‌ی مهم درباره‌ی حذف حساب
اپ سمت کاربر (کلاینت) به‌تنهایی نمی‌تواند حساب Firebase Auth یک کاربر دیگر
را کامل حذف کند — این محدودیت خود Firebase است، نه این برنامه. از پنل
ادمین می‌توانی لایسنس را «باطل» کنی که همان بستن دسترسی/قفل‌شدن برنامه‌ی
آن کاربر را نتیجه می‌دهد. برای حذف کامل حساب از Firebase Auth، باید از
کنسول Firebase (Authentication → Users) اقدام کنی.
