# راه‌اندازی سیستم لایسنس «حسابدار»

این پکیج شامل ۳ تغییر روی پروژه‌ی اصلی است:

1. `firestore.rules` — قوانین امنیتی: سینک ابری هر کاربر فقط وقتی کار می‌کند
   که یک سند `licenses/{uid}` با وضعیت فعال/آزمایشی داشته باشد.
2. `admin/index.html` — پنل ادمین (صدور، تمدید، لغو لایسنس).
3. تغییرات کوچک در `app.js` / `index.html` / `style.css` — یک جعبه‌ی
   «وضعیت لایسنس» + «کد اتصال (UID)» در تنظیمات ← همگام‌سازی دو گوشی.

همه‌چیز روی **پلن رایگان Firebase (Spark)** کار می‌کند؛ نیازی به کارت
بانکی یا Cloud Functions نیست.

---

## ۱) پیش‌نیاز

```bash
npm install -g firebase-tools
firebase login
```

از پوشه‌ی همین پکیج (که `firebase.json` داخلش هست):

```bash
firebase use hesabdari-fd3a3
```

اگر پروژه به این نام قبلاً به حساب Firebase شما وصل نبود، به‌جایش:

```bash
firebase use --add
```
و پروژه‌ی `hesabdari-fd3a3` (همان که در app.js هست) را انتخاب کنید.

---

## ۲) ساخت اکانت ادمین

1. به [Firebase Console](https://console.firebase.google.com) → پروژه →
   **Authentication → Users → Add user** بروید.
2. یک ایمیل و رمز برای خودتان (ادمین) بسازید — این جدا از اکانت‌های
   مشتری‌هاست.
3. کنار همان کاربر تازه‌ساز، مقدار **User UID** را کپی کنید.

## ۳) ثبت ادمین در قوانین امنیتی

فایل `firestore.rules` را باز کنید و خط زیر را با UID خودتان جایگزین
کنید:

```
function isAdmin() {
  return isSignedIn() && request.auth.uid in [
    "REPLACE_WITH_YOUR_ADMIN_UID_1"
  ];
}
```

اگر چند نفر باید دسترسی ادمین داشته باشند، UID هرکدام را به همان
آرایه اضافه کنید.

## ۴) استقرار (Deploy)

```bash
firebase deploy --only firestore:rules
firebase deploy --only hosting
```

بعد از این دستور دو آدرس در دسترس‌اند (دامنه دقیق را خود دستور چاپ
می‌کند):

- `https://hesabdari-fd3a3.web.app/` → خود اپ حسابدار
- `https://hesabdari-fd3a3.web.app/admin/` → پنل ادمین لایسنس

پنل ادمین را جایی که مشتری‌ها به آن نمی‌رسند نگه دارید (مثلاً فقط
لینکش را برای خودتان بوکمارک کنید؛ صفحه `noindex` است ولی عمومی و
بدون رمز عبور مرورگر در دسترس است — ورودش با ایمیل/رمز ادمین محافظت
می‌شود، همین کافیست).

---

## ۵) گردش کار فروش لایسنس (روزمره)

1. مشتری اپ را نصب می‌کند، وارد **تنظیمات ← همگام‌سازی دو گوشی** می‌شود
   و یک حساب سینک (ایمیل/رمز) می‌سازد.
2. زیر همان بخش، جعبه‌ی «کد اتصال» UID او را نشان می‌دهد؛ مشتری آن را
   کپی کرده و بعد از پرداخت (مثلاً از طریق زرین‌پال یا آیدی‌پی) برایتان
   در تلگرام/واتساپ می‌فرستد.
3. شما در `/admin/` وارد می‌شوید، همان UID را در فرم بالای صفحه
   می‌گذارید، وضعیت را `فعال` می‌کنید، پلن و تاریخ انقضا را می‌زنید و
   ذخیره می‌کنید.
4. همان لحظه، اپ مشتری (چون سند لایسنسش را realtime گوش می‌دهد) وضعیت
   را «فعال» نشان می‌دهد و سینک ابری کار می‌کند.
5. برای لغو (مثلاً چارج‌بک یا پایان اشتراک)، در جدول پنل ادمین دکمه‌ی
   «لغو» را بزنید.

---

## نکات مهم

- **این یک قفل واقعی روی داده‌ی ابری است، نه روی خود اپ.** اپ حسابدار
  به‌صورت آفلاین هم کامل کار می‌کند (این طراحی عمدی خود پروژه است)، پس
  کاربری که لایسنس ندارد باز هم می‌تواند از اپ به‌صورت محلی/آفلاین
  استفاده کند — فقط **سینک دو گوشی و پشتیبان ابری** برایش قفل می‌شود.
  اگر می‌خواهید کل اپ هم قفل شود، باید معماری را عوض کنید (مثلاً یک
  سرور واسط برای تمام داده‌ها) که پیچیدگی و هزینه‌ی نگه‌داری بیشتری دارد.
- کد اتصال (UID) یک شناسه‌ی تصادفی است، نه رمز عبور کسی — امن است که
  کاربر آن را برای شما بفرستد.
- برای گرفتن UID یک کاربر تستی خودتان: در خود اپ وارد تنظیمات شوید،
  یک حساب سینک بسازید، و از همان جعبه‌ی «کد اتصال» کپی کنید.

## بهبودهای بعدی (اختیاری)

- **دوره‌ی آزمایشی خودکار:** می‌شود قانونی اضافه کرد که به هر کاربر تازه
  (وقتی هنوز سند `licenses/{uid}` ندارد) اجازه بدهد خودش یک سند با
  `status:"trial"` و انقضای حداکثر ۷ روز برای خودش بسازد — بدون دخالت
  ادمین. این نیاز به یک قانون دقیق‌تر در `firestore.rules` دارد که
  فقط ساخت اول را با محدودیت تاریخ اجازه می‌دهد؛ اگر خواستید همین را
  هم برایتان می‌نویسم.
- **تایید خودکار پرداخت:** وصل کردن وبهوک زرین‌پال/آیدی‌پی به یک
  Cloud Function که خودش سند لایسنس را می‌سازد (نیاز به پلن Blaze
  دارد چون Cloud Functions رایگان نیست، هرچند سهمیه‌ی رایگان ماهانه‌ی
  قابل‌توجهی دارد).
